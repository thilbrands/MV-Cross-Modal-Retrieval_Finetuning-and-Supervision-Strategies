# System-Abhängigkeit (nicht per pip):
# - ffmpeg (mit HTTPS/OpenSSL) für downloader.py (yt-dlp --download-sections).
#   Auf dem Cluster: Conda-Env (z.B. conda_ffmpeg) oder module load FFmpeg.

# Pipeline: Download (downloader.py)
yt-dlp>=2024.1

# Pipeline: Extraktion + Embedding (extract_and_embed_videos.py) – CLIP + Wav2CLIP, keine Frames/Audio auf Disk
numpy>=1.24
opencv-python>=4.8
av>=12.0
librosa>=0.10
tqdm>=4.65
scikit-learn>=1.3
matplotlib>=3.7
torch>=2.0
torchvision>=0.15
Pillow>=9.0
wav2clip>=0.1
# CLIP: gleiche API wie OpenAI (import clip, clip.load("ViT-B/32")), ohne Git
clip-anytorch>=2.0
